{
  "version": 2,
  "buildCommand": "rm -rf dist && npm run build",
  "outputDirectory": "dist/public",
  "framework": null,
  "cleanUrls": true,
  "functions": {
    "api/index.ts": {
      "runtime": "nodejs18.x"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    }
  ]
}
